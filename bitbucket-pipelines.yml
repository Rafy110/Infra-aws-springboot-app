image: node:18-alpine

definitions:
  steps:
    - step: &build-and-test
        name: Build and Test
        caches:
          - node
        script:
          - cd app
          - npm ci
          - npm run build
          - npm run lint || true

    - step: &build-and-push-dev
        name: Build and Push Docker Image (Dev)
        services:
          - docker
        script:
          - |
            # Install AWS CLI v2 (Alpine package)
            apk add --no-cache aws-cli
            
            # Configure AWS CLI
            aws configure set aws_access_key_id $AWS_ACCESS_KEY_ID
            aws configure set aws_secret_access_key $AWS_SECRET_ACCESS_KEY
            aws configure set region $AWS_REGION
            
            # Get ECR registry URL
            ECR_REGISTRY=$(aws ecr describe-repositories --repository-names ${APP_NAME}-dev --query 'repositories[0].repositoryUri' --output text | cut -d'/' -f1)
            
            # Login to ECR
            aws ecr get-login-password --region $AWS_REGION | docker login --username AWS --password-stdin $ECR_REGISTRY
            
            # Build and push
            cd app
            docker build -t ${APP_NAME}-dev:${BITBUCKET_COMMIT} .
            docker tag ${APP_NAME}-dev:${BITBUCKET_COMMIT} ${ECR_REGISTRY}/${APP_NAME}-dev:${BITBUCKET_COMMIT}
            docker push ${ECR_REGISTRY}/${APP_NAME}-dev:${BITBUCKET_COMMIT}
            echo "✅ Image pushed!"

    - step: &deploy-dev
        name: Deploy to ECS (Dev)
        script:
          - |
            # Install AWS CLI v2
            apk add --no-cache aws-cli
            
            # Configure AWS
            aws configure set aws_access_key_id $AWS_ACCESS_KEY_ID
            aws configure set aws_secret_access_key $AWS_SECRET_ACCESS_KEY
            aws configure set region $AWS_REGION
            
            # Deploy
            aws ecs update-service \
              --cluster ${APP_NAME}-dev-cluster \
              --service ${APP_NAME}-dev-service \
              --force-new-deployment \
              --region $AWS_REGION
            echo "✅ Deployed!"